# Docusaurus environment example
# Copy to .env and adjust values.

# Base site URL (production)
SITE_URL=https://docs.vessel.local
API_URL=http://localhost:8000/api
# Base path if served from subpath (must start and end with /). For root, use /
BASE_URL=/

# Google Analytics measurement ID (leave blank if not used)
GTM_CONTAINER_ID=
GA_MEASUREMENT_ID=

# Hotjar settings (optional)
HOTJAR_ID=
HOTJAR_VERSION=6

# Algolia DocSearch (optional)
ALGOLIA_APP_ID=
ALGOLIA_API_KEY=
ALGOLIA_INDEX_NAME=
